# Copia este archivo a .env y reemplaza los valores reales
# Windows/PowerShell: este archivo NO se "ejecuta" en terminal; es leído por python-decouple

# === Cloudflare Turnstile settings ===
CLOUDFLARE_TURNSTILE_ENABLED=True
CLOUDFLARE_TURNSTILE_SITE_KEY=0x4AAAAAAB54hAon_4XgENN8
CLOUDFLARE_TURNSTILE_SECRET_KEY=0x4AAAAAAB54hBDY7KEoKS-s_ZOcY2YXBUA

# === Sistema JWT PURO ===
# Configuración JWT (opcional - valores por defecto en settings.py)
JWT_SECRET_KEY=your-jwt-secret-key-here
# JWT_ACCESS_TOKEN_LIFETIME=15     # minutos (default: 15)
# JWT_REFRESH_TOKEN_LIFETIME=5     # días (default: 5)
# JWT_ROTATE_REFRESH_TOKENS=False  # (default: False - SIN rotación)

# === Django basics ===
DEBUG=True
SECRET_KEY=django-insecure-change-me-in-production
ALLOWED_HOSTS=localhost,127.0.0.1
# === DB (ajusta a tu entorno) ===
DB_NAME=gestion_practicas
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# === Email (opcional) ===
EMAIL_ENABLED=False
DEFAULT_FROM_EMAIL=no-reply@upeu.edu.pe

# === Otros ===
JWT_SECRET_KEY=change-me
